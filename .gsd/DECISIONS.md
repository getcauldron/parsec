# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Desktop framework | Tauri v2 | Cross-platform, tiny bundle size (~10MB vs Electron's 100MB+), native WebView, Rust core | No |
| D002 | M001 | arch | OCR engine default | PaddleOCR (PP-OCRv5) | Better accuracy than Tesseract on complex layouts, small models (~15MB), user chose this | No |
| D003 | M001 | arch | OCR engine architecture | Swappable interface | User explicitly didn't want to marry to one engine; abstract OcrEngine interface | No |
| D004 | M001 | arch | PDF generation | OCRmyPDF | Handles PDF/A, text layer placement, optimization — no reason to reimplement PDF plumbing | Yes — if OCRmyPDF proves too rigid |
| D005 | M001 | arch | Python ↔ Tauri communication | Sidecar via stdin/stdout JSON | Tauri's native sidecar API, proven pattern, avoids HTTP overhead | Yes — if IPC becomes bottleneck |
| D006 | M001 | arch | Python packaging | PyInstaller | Compiles Python + deps into standalone binary per platform, Tauri bundles it | Yes — if PyInstaller causes issues (Nuitka as fallback) |
| D007 | M001 | pattern | Output naming | `_ocr.pdf` suffix next to original | Predictable, no data loss, no configuration needed | No |
| D008 | M001 | pattern | File processing mode | Per-file (1 input → 1 output) | Simple mental model, no merge decisions | Yes — if batch merge added later |
| D009 | M001 | scope | Cross-platform verification | macOS primary in M001, all platforms in M002 | Dev is on macOS; cross-platform packaging is its own problem | No |
| D010 | M001 | scope | Quality assurance | Test fixture set with CER/WER thresholds | Catches regressions automatically, curated for real use cases | No |
| D011 | S01 | pattern | Test fixtures | Synthetic generated images | Perfect ground truth, reproducible, no copyright issues — prefer over real scans for baseline | Yes — add real scans later for realism |
| D012 | S01 | pattern | PDF output type | `--output-type pdf` not `pdfa` | Avoids Ghostscript AGPL dependency for now; revisit PDF/A when distribution model is clear | Yes — switch to PDF/A if licensing resolved |
| D013 | S01 | pattern | ocrmypdf-paddleocr plugin | Vendor key logic if incompatible | Plugin has no PyPI release, 11 commits, uncertain PaddleOCR 3.x compat — vendor rather than depend on external | Yes — use published package if it matures |
| D014 | S01 | infra | Python runtime | Python 3.13 venv (backend/.venv) | System Python is 3.9; PaddlePaddle 3.x requires >=3.10. Homebrew Python 3.13 available, PaddlePaddle 3.3.0 supports it | Yes — pin to 3.12 if 3.13 compat issues arise |
| D015 | S01 | pattern | PaddleOCR init strategy | Lazy init with stdout suppression | PaddleOCR dumps noisy C++ output during init/predict; contextlib.redirect_stdout/stderr to devnull. Init on first recognize() call (~4-5s cold start) | No |
| D016 | S01 | infra | ocrmypdf-paddleocr plugin | Use published pip package (v0.1.1) | Plugin works with PaddleOCR 3.2.0/PaddlePaddle 3.2.2 — pins older versions but predict() API is compatible. No vendoring needed. | Yes — vendor if plugin breaks with future PaddleOCR updates |
| D017 | S01 | pattern | Language code mapping | Pipeline maps short codes (en) to Tesseract ISO 639-2 (eng) | OcrOptions uses short codes user-facing; OCRmyPDF + plugin validate against Tesseract lang list. Pipeline._to_tesseract_lang() bridges them | No |
| D018 | S01 | pattern | Multi-column ground truth order | Interleaved by row (left-1, right-1, left-2, right-2) | PaddleOCR reads top-to-bottom, left-to-right across columns. Ground truth must match reading order for meaningful CER/WER. Sequential (all-left-then-all-right) inflates error rates to ~60% even with perfect character recognition | No |
| D019 | S01 | infra | jiwer API version | Use jiwer 4.x `reference_transform` param | jiwer 4.0.0 renamed `truth_transform` to `reference_transform`. `cer()` needs `ReduceToListOfListOfChars`, `wer()` needs `ReduceToListOfListOfWords` | No |
| D020 | S02 | pattern | Tauri scaffold method | Manual scaffold instead of `pnpm create tauri-app` | Interactive scaffolder conflicts with existing `backend/` directory and can't be run non-interactively. Created equivalent files directly. | No |
| D021 | S02 | pattern | Sidecar binary target-triple copy | build.rs copies `parsec-sidecar` → `parsec-sidecar-{triple}` in target dir | `tauri_build::build()` strips the triple suffix during copy but `ShellExt::sidecar()` appends it at runtime. build.rs bridges the gap. | No |
| D022 | S02 | pattern | Sidecar command/response pattern | Event-driven via mpsc channel with 5s timeout | `greet_sidecar` registers a one-shot event listener, sends JSON to stdin, waits on mpsc channel. Avoids polling, handles timeouts cleanly. | Yes — may need request-id correlation for concurrent commands |
| D023 | S02 | pattern | PyInstaller entrypoint separation | Separate sidecar_entry.py from sidecar.py | sidecar_entry.py handles binary-specific concerns (stdout buffering, env var noise suppression) then calls sidecar.main(). Keeps sidecar.py testable without PyInstaller. | No |
| D024 | S02 | pattern | Sidecar launcher with fallback | Wrapper script tries PyInstaller binary, falls back to venv Python | Keeps `cargo tauri dev` working whether or not the binary has been built. Developers who haven't run build_sidecar.sh still get a working dev loop. | Yes — remove fallback when CI always builds the binary |
| D025 | S03 | pattern | Progress streaming via Channel, not global events | Use `tauri::ipc::Channel<T>` scoped to the `process_files` command instead of global `app.emit()` | Ordered, typed, scoped to the invoking call — avoids race conditions with concurrent commands. Global events kept for `greet_sidecar` backward compat. | No |
| D026 | S03 | pattern | Stage-based progress, not percentage | Progress events use stages (queued/initializing/processing/complete/error) not percentages | OCRmyPDF has no progress callback for single images. 2-5s processing time makes a progress bar theatrical. Stages are honest. | Yes — add percentage if pipeline is extended with multi-step preprocessing |
| D027 | S03 | pattern | Output path computed in sidecar | Python sidecar computes `_ocr.pdf` output path, not the Rust layer | Keeps path logic close to the pipeline, avoids path-encoding issues across IPC, single source of truth for naming convention | Yes — move to Rust if sidecar becomes stateless |
| D028 | S03 | pattern | Channel registry for progress routing | Rust uses `HashMap<String, Sender>` keyed by request UUID to route sidecar stdout events to the correct Channel | Supports concurrent commands (future) while keeping sidecar sequential. Non-progress responses continue via global event broadcast. | No |
| D029 | S03 | pattern | Enrich forwarded progress events with input_path | Rust injects `input_path` into each Channel event before forwarding to frontend | Sidecar events only carry UUID id. Frontend needs file path for correlation. Adding at the Rust layer keeps sidecar protocol minimal and avoids redundant data in sidecar responses. | No |
| D030 | S03 | ui | Industrial/utilitarian dark aesthetic | Dark bg (#1a1a1e), DM Mono + DM Sans, amber processing states, green completion | Document utility needs to feel purposeful and trustworthy. Control-panel metaphor with warm amber accents distinguishes from generic blue-on-white. | Yes — user may request different theme |
| D031 | S03 | pattern | Sidecar launcher root detection | Search for `backend/` directory instead of `backend/dist/parsec-sidecar` | Old logic failed when PyInstaller binary was absent — walked too far up the tree and broke venv fallback path resolution. | No |
| D032 | S04 | scope | Ship 49 languages, not 80+ | Use only the 49 languages mapped by the ocrmypdf_paddleocr plugin | R007 says "80+ languages" but the plugin only maps 49 Tesseract codes. Extending requires either modifying a third-party plugin or bypassing its validation. 49 covers all major languages. Document the gap, extend later if needed. | Yes — extend mapping to reach 80+ in a follow-up |
| D033 | S04 | pattern | Settings persistence via Tauri store plugin | Use `@tauri-apps/plugin-store` for language and settings persistence | Official Tauri v2 approach, auto-save, key-value API, handles app data paths. No reason to build custom persistence. | No |
| D034 | S05 | arch | No separate pdf_input.py or preprocessing.py | Use OCRmyPDF's native PDF handling and preprocessing kwargs instead of building separate modules | OCRmyPDF handles PDF→image→OCR→PDF internally and exposes deskew/rotate/clean as kwargs. Separate modules would duplicate logic and apply operations in the wrong order. Boundary map mentioned these files but research proved them unnecessary. | No |
| D035 | S05 | pattern | PDF input default mode | `skip_text=True` for PDF inputs, no mode flag for images | Prevents `already_done_ocr` errors on PDFs with existing text layers. Images always need OCR so no mode flag is needed. `skip_text` and `force_ocr` are mutually exclusive in OCRmyPDF. | Yes — add `force_ocr` as advanced user option later |
| D036 | S05 | pattern | Preprocessing toggles default off | Deskew, rotate, clean all default to `false` | Clean requires `unpaper` system binary which may not be installed. Deskew/rotate add processing time. Users opt-in to preprocessing they need. | Yes — could default deskew on if benchmarks show consistent improvement |
| D037 | M002 | scope | Code signing deferred | Ship unsigned for now | Apple Developer Program ($99/yr) and Windows Authenticode certs ($200-500/yr) are ongoing costs for a free project. Unsigned apps work — macOS Gatekeeper allows open via right-click, Windows SmartScreen is dismissible. Revisit if download friction becomes a real problem. | Yes — add signing when user base justifies cost |
| D038 | M002 | scope | Cross-platform verification deferred to M003 | Prove distribution on macOS only in M002 | Can only build macOS sidecar locally. Windows/Linux sidecar binaries require platform-specific CI runners from M003. M002 proves the bundling pattern on macOS; M003 extends to all platforms. | No |
| D039 | M002 | arch | PyInstaller _internal/ via bundle.resources | Use `bundle.resources` instead of switching to `--onefile` | `--onefile` has a known process-kill bug where Tauri can't kill the PyInstaller bootloader's child process. `--onedir` + `resources` avoids this, has faster startup, and the sidecar module already handles process lifecycle. | Yes — reconsider if bundle size is unacceptable |
| D040 | M002 | arch | Auto-update via GitHub Releases | `tauri-plugin-updater` + GitHub Releases endpoint | Official Tauri v2 approach. M002 wires the client-side plugin and config; M003's release workflow publishes `latest.json` + signed artifacts. No custom update server needed. | Yes — switch to custom server if GitHub Releases proves insufficient |
| D041 | M002/S01 | arch | _internal/ via bundle.macOS.files, not bundle.resources | Use `bundle.macOS.files` with `"MacOS/_internal"` key | `bundle.resources` puts files in `Contents/Resources/` but PyInstaller binary expects `_internal/` as a sibling in the same directory (`Contents/MacOS/`). `bundle.macOS.files` maps directly into `Contents/`, letting us place `_internal/` exactly where the binary looks for it. This eliminates the need for a sidecar launcher wrapper or path resolution changes in production. | **Superseded by D043** |
| D042 | M002/S01 | pattern | build.rs profile-gated sidecar copy | Copy PyInstaller binary only in release profile | Dev mode (`cargo tauri dev`) uses the committed shell wrapper script which resolves to the local PyInstaller output or venv Python. Release builds (`cargo tauri build`) copy the real PyInstaller binary over the wrapper so Tauri bundles the actual Mach-O executable. Profile check (`PROFILE == "release"`) prevents dev mode from accidentally using the PyInstaller binary which would fail without `_internal/` in the target dir. | No |
| D043 | M002/S01 | arch | _internal/ mapped to Contents/Frameworks/, not Contents/MacOS/_internal/ | Use `bundle.macOS.files` with `"Frameworks"` key | PyInstaller bootloader detects `.app/Contents/MacOS/` path and remaps `_MEIPASS` to `Contents/Frameworks/` (macOS app bundle convention). Placing `_internal/` in `Contents/MacOS/_internal/` fails — bootloader looks for `Python` at `Contents/Frameworks/Python`. Mapping `_internal/` source to `Frameworks` in `bundle.macOS.files` puts all PyInstaller runtime dependencies exactly where the bootloader expects them. | No |
| D044 | M002/S03 | ui | Visual identity retheme replacing D030 | Black/off-white/emerald-green palette, new geometric typography, white processing accents | Icon (`icon.png`) establishes the brand: black bg, off-white P, emerald green accent. D030's amber industrial aesthetic is replaced to align with this identity. Processing states use white/off-white, completion stays green. Bundle icons regenerated from icon.png. | **Supersedes D030** |
| D045 | M002/S01 | pattern | PyInstaller build needs --collect-data paddlex and --copy-metadata for ocr-core deps | Explicit metadata collection for paddlex ecosystem | PaddleX checks `importlib.metadata` at runtime for its `ocr-core` extra deps (imagesize, opencv-contrib-python, pyclipper, pypdfium2, shapely). PyInstaller collects modules but not dist-info by default. Without metadata, PaddleX raises DependencyError. | No |
| D046 | M002/S01 | pattern | PaddleX offline model patch in sidecar_entry.py | Monkey-patch _ModelManager._get_model_local_path to check local cache before network | PaddleX 3.x requires network health checks to model hosters before loading models, even when cached locally. HTTPS health checks fail in PyInstaller bundles. Patch checks `~/.paddlex/official_models/{model_name}` on disk first, falling back to original network behavior. Models must be pre-cached. | Yes — remove if PaddleX fixes offline support upstream |
| D047 | M002/S02 | config | createUpdaterArtifacts value | `true` (boolean), not `"v2"` (string) | Tauri config schema defines Updater as `boolean \| "v1Compatible"`. String `"v2"` fails with "data did not match any variant of untagged enum Updater". Use `true` for v2 updater artifacts. | No |
| D048 | M002/S02 | infra | TAURI_SIGNING_PRIVATE_KEY_PASSWORD for passwordless keys | Set `TAURI_SIGNING_PRIVATE_KEY_PASSWORD=""` explicitly | Even with a key generated without password, the build fails with "Device not configured (os error 6)" if the PASSWORD env var is absent — it tries to read from stdin. CI and local builds must export the empty string. | No |
